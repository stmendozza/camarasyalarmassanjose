---
import PageLayout from '../layouts/PageLayout.astro';
import { getPageMeta } from '../data/pages-meta';
import { getEntry } from 'astro:content';

const meta = getPageMeta('instalacion');
const service = await getEntry('services', 'instalacion');
---

<PageLayout
	title={meta.title}
	description={meta.description}
	h1={meta.h1}
	lead={service?.data.summary}
	pathname="/instalacion"
>
	<p class="text-slate">Proceso de instalación — Fase 5.</p>
</PageLayout>

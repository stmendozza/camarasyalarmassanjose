# QA — Lighthouse, contraste y teclado — 2026-09-25

Medición sobre el build de producción (`astro preview`), formulario móvil de Lighthouse (412×823, DPR 1.75). No sustituye una pasada en un teléfono físico.

## Lighthouse (móvil)

| Página | Performance | Accesibilidad | SEO |
|--------|-------------|---------------|-----|
| `/` | 100 | 100 | 100 |
| `/contacto` | 100 | 100 | 100 |

Home: FCP 1.1 s, LCP 1.7 s. Umbral de la fase: ≥ 90 en las tres categorías.

## Contraste

Tokens de texto sobre `--bg` (`#1c211e`) superan 4.5:1 (`--fg` 14.9, `--fg-muted` 8.8, `--green` 7.3). El punto del panel lima pasó de `--coral` (`#ff5a6a`, 1.6:1) a `#9c2430` (4.0:1) porque el coral claro no se leía sobre `#8fcb6a`. Los enlaces dentro de un párrafo llevan subrayado, no solo color.

## Teclado

- Enlace «Saltar al contenido».
- `:focus-visible` en verde.
- El menú móvil se cierra con Escape y devuelve el foco al botón. El botón dice Menú / Cerrar y usa `aria-expanded`.
- El control de WhatsApp del header mide al menos 44×44 px.

## Teléfono real

El cliente confirmó el 2026-09-25, después de ver el sitio en su teléfono, que se ve bien. Con eso cierra la fase 7.
